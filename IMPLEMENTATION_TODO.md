# v1.0 beta 移植接入清单

已确认目标环境：
- Slay the Spire 2 beta `v0.111.0`
- RitsuLib `0.5.19`
- .NET `9.0`

1. 已完成：安装 .NET 9 SDK，建立 `WeiDoctor.csproj`，引用游戏 `sts2.dll`、RitsuLib `lib/0.111.0/STS2-RitsuLib.dll`、GodotSharp 与 Harmony。
2. 已完成：增加基于当前 RitsuLib 的最小真实入口 `Entry.Initialize()`，注册模组程序集、卡池、遗物池、部署关键词、测试卡与调度中心遗物。
3. 已完成：复制到当前 beta 实际扫描的 `workshop/content/2868840/3747602295/WeiDoctorLocal`，并确认 `WeiDoctor.load.log` 生成。
4. 已完成：注册博士角色、初始遗物和基础打防卡。
5. 已部分完成：注册第一批 I 阶干员测试卡：
   - 隐现
   - 角峰
   - 惊蛰
   - 深巡
   - 普罗旺斯
   - 古米
   其余 `data/cards.json` 中的普通干员池与特殊来源池待批量接入。
6. 用 RitsuLib 关键词注册补齐术语 hover：部署、晋升、天资、筹措、灼燃、炎佑、缓慢、冻结、各盟约等。
7. 在卡牌奖励、商店、调度中心奖励生成处加入等阶过滤。
8. 接入休息处自由行动：升级调度中心。
9. 已部分完成：接入调度中心内部部署队列：
   - 打出非能力干员后先正常结算战斗效果，再进入部署。
   - 满3格时撤退最早部署者，复制回弃牌堆。
   - 回合结束按部署顺序触发部署效果。
   - 部署回合归零时复制回抽牌堆。
   - 当前部署人数用调度中心遗物计数显示；专门的三格部署 UI 待后续实现。
10. 能力牌不部署，改为添加本场战斗持续状态。
11. 接入 `PromotionService`：
   - 监听获得卡牌、奖励选择、商店购买和事件获得。
   - 发现3张同名同阶时，移除3张并加入晋升版本。
   - 附赠当前调度中心等级+1三选一奖励。
12. 接入 RitsuLib 设置页：
   - 随机模式开关。
   - 初始候选展示数量。
   - 部署位数量。
   - 晋升所需同名卡数量。
   - 调度中心费用参数。
13. 生成 `WeiDoctor.pck` 或确认无 PCK 模式下的素材加载策略，更新 `WeiDoctor.json` 后进行完整游戏测试。
